package com.atlassian.webtest.ui.keys;

/**
 * Modifier keys are keys that, when pressed, modify the normal actions of another keys. Those can be
 * added to any entered {@link KeySequence} in the framework.
 *
 * @since 4.3
 */
public enum ModifierKey
{
    SHIFT,
    ALT,
    CONTROL,
    META
}
